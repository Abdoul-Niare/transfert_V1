{% extends 'base.html.twig' %}

{% block title %}Modifier le transfert
{% endblock %}

{% block body %}
	<section class=" Edition_transfert ">
	<div class="container mt-4">
		<h2 class="text-center mt-5">Modification du Transfert :{{transfert.id}}</h2>
		{{ include('transfert/_form.html.twig', {'button_label': 'Confirmer'}) }}
		{# <a class="btn btn-secondary p-1 m-1" href="{{ path('app_transfert_index') }}">Revenir à la liste</a>
         <div class="text-start">
            {{ include('transfert/_delete_form.html.twig') }}
        </div> #}
	</div>
	</section>
	
{% endblock %}
