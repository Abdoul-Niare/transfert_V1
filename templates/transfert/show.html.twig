{% extends 'base.html.twig' %}

{% block title %}Transfert_Show
{% endblock %}

{% block body %}
	<h1> Transfert {{ transfert.statut }}</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Transfert: {{ transfert.codeSecret }}</th>
				<td>{{ transfert.id }}</td>
			</tr>
			<tr>
				<th>Status</th>
				<td>{{ transfert.statut }}</td>
			</tr>
			<tr>
				<th>Code secret</th>
				<td>{{ transfert.codeSecret }}</td>
			</tr>
			<tr>
				<th>Montant du transfert</th>
				<td>{{ transfert.montTransfert }}</td>
			</tr>
			<tr>
				<th>Montant pour le Bénéficiaire</th>
				<td>{{ transfert.montBenef }}</td>
			</tr>
			<tr>
				<th>Frais de transfert</th>
				<td>{{ transfert.fraisTransfert }}</td>
			</tr>
			<tr>
				<th>Commission du site</th>
				<td>{{ transfert.comTransfert }}</td>
			</tr>

			{# <?php if($this->getUser()->roles === "ROLE_AGENT"){ ?> #}
			<tr>
				<th>Commission de l'Agent_Livreur</th>
				<td>{{ transfert.comAgentLivreur }}</td>
			</tr>

			{# <?php } ?> #}

			<tr>
				<th>Nom du bénéficiaire</th>
				<td>{{ transfert.nomBenef }}</td>
			</tr>
			<tr>
				<th>Cate d'identité du bénéficiaire</th>
				<td>{{ transfert.numBenef }}</td>
			</tr>
			<tr>
				<th>Telephone du bénéficiaire</th>
				<td>{{ transfert.numTelBenef }}</td>
			</tr>
			<tr>
				<th>Date d'envoi</th>
				<td>{{ transfert.dateEnvoi ? transfert.dateEnvoi|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
			<tr>
				<th>Date de pris en charge</th>
				<td>{{ transfert.datePrisCharge ? transfert.datePrisCharge|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
			<tr>
				<th>Date de livraison</th>
				<td>{{ transfert.dateLivr ? transfert.dateLivr|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>
	<div class="text-center">
		{% if is_granted('ROLE_ADMIN') %}
		<a class="btn btn-warning p-1" href="{{ path('app_transfert_take', {'id': transfert.id}) }}">Prendre en charge</a>
		<a class="btn btn-warning p-1" href="{{ path('app_transfert_delivery', {'id': transfert.id}) }}">Livrer le transfert</a>
		{% endif %}
		<a class="btn btn-primary p-1 " href="{{ path('app_transfert_index') }}">Retour à la liste</a>
		{# <a class="btn btn-secondary p-1 " href="{{ path('app_transfert_edit', {'id': transfert.id}) }}">Modifier</a> #}
	</div>
    
    
	{# <div class="text-center">
		<a class="btn btn-warning p-1 m-1" href="{{ path('app_transfert_delivery', {'id': transfert.id}) }}">transfert livré</a>
	</div> #}

	
	{# {{ include('transfert/_confirm_take_form.html.twig') }} #}

	{{ include('transfert/_delete_form.html.twig') }}

{# {{ include('transfert/_form.html.twig', {'button_label': 'Confirmer'}) }} #}
	{# // <script>
	    // function fun() {
	    //     (confirm("Confirmez-vous le transfert?"));
	    // }</script>
	
	    // onclick="fun()" #}
{% endblock %}
