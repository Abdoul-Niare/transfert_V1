{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur
{% endblock %}

{% block body %}

	<section class="vh-60 p-0 m-0 ">
		{% for message in app.flashes('success') %}
			<div class="alert alert-success ">
				<strong class="font-bold">Succès !</strong>
				{{ message }}
			</div>
		{% endfor %}
		<h2 class=" mt-5 text-center ">Modification de l'utilisateur</h2>

		<div class="container vh-80 mt-4 vw-60">
			{{ include('user/_form.html.twig', {'button_label': 'Enrégistrer'}) }}

			<div class="text-center mt-0">
				{{ include('user/_delete_form.html.twig') }}
			</div>
		</div>
	</section>

{% endblock %}
