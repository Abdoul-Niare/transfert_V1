{{ form_start(form) }}
		<div class="row d-flex justify-content-center justify-content ">
			<div class="col-3">

				{{ form_row(form.nom) }}
				{{ form_row(form.mail) }}

			</div>
			<div class="col-3">
				{{ form_row(form.Prenom) }}
				{{ form_row(form.username) }}
			</div>
			<div class="col-3">
				{{ form_row(form.ville) }}
				{{ form_row(form.roles) }}
			</div>
			<div class="d-flex justify-content-center mt-3">
				{{ form_row(form.reset_password) }}
			</div>
		</div>
		<div class="d-flex justify-content-center ">
			
				<button class="btn btn-success p-1 m-1">{{ button_label|default('Enregistrer') }}</button>
				<a class="btn btn-info p-1 m-1" href="{{ path('app_user_index') }}">Annuler</a>
			
		</div>
{{ form_end(form) }}
	




