<nav class="navbar navbar-expand-lg navbar-dark ">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('app_home')}}">
			<img src="{{asset('img/E-CFA.png')}}" alt="logo" style=" width: 45px; border-radius:50%; font-size: 30px;">
			SEND-€-CFA
		</a>

		<div class="navbar-nav ">
			<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{ path('app_transfert_new')}}">Envoyer de l'argent</a>
			<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{ path('contact.index') }}">Devenir un agent</a>

			{% if app.user %}
				{% if is_granted('ROLE_ADMIN') %}
					<!-- Infos Menu Admin -->
					<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{ path('app_transfert_index')}}">Gestion des transferts</a>
					<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{ path('app_user_index')}}">Gestion des utilisateurs</a>
					<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{ path('app_ville_index')}}">Gestion des villes</a>

				{% elseif is_granted('ROLE_PARTNER') %}
					<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{ path('app_transfert_compte')}}">Mon compte</a>
					<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{ path('app_transfert_index')}}">Les transferts</a>
				{% elseif is_granted('ROLE_USER') %}
					<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{ path('app_transfert_index')}}">Mes transferts</a>

				{% endif %}

				<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{ path('user_profile_modifier')}}">
					<i class="fa-solid fa-user" id="icone"></i>
					{{app.user.nom}}


					<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{path('app_logout')}}">
						<i class="fas fa-power-off"></i>
						Déconnexion</a>
				{% else %}
					<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{path('app_register')}}">S'inscrire</a>
					<a class="btn btn-outline-success m-1 text-center" style="color:white; border:none" href="{{path('app_login')}}">Se connecter</a>
					{# <a class="btn btn-outline-light ms-2 me-2"style="color:rgb(186,124,51)" href="{{path('app_login')}}">Envoyer de l'argent</a> #}
				{% endif %}
			</div>
		</div>
	</nav>
